from setuptools import find_packages, setup

setup(
    name = "vvlib",
    packages = find_packages(include=["vvlib"]),
    version = "0.0.1",
    description = "Random functions",
    author = "Vojtech Velebny"
)